{
  "name": "@pepe.black/chumm-uffa-interface",
  "version": "0.0.2",
  "description": "chumm-uffa communication interface",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && npm run tsc && npm pack",
    "test": "npm run build && mocha -t 30000 dist/tests/**/*.spec.js --exit",
    "tsc": "tsc",
    "clean": "rimraf dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.0.10",
    "@types/mocha": "^2.2.44",
    "chai": "^4.1.2",
    "mocha": "^4.0.1",
    "rimraf": "^2.6.2",
    "typescript": "^2.6.2"
  }
}
